# v1.6-B 首批实现独立只读复审

日期：2026-09-06
连接入口：`Codex with ChatGPT · Evolune-v1.2 · yingqiu.me`
工作区：`Evolune-v1.2`
分支/基线：`main` / `df12329278eafa488713edf202554cdbd523b8d0`
工作树：dirty；未创建提交；复审者未修改文件

## 首轮结论

首轮结论为 `REQUEST_CHANGES`，无 P0。P1 指出 `WidgetWork.kt` 的 action-time
availability 只用单个目标 occurrence 派生，会让 legacy/null-slot 事件在同日 sibling
之间错误归属；P2 指出 OPEN_APP 证据覆盖范围、`TODO.MD` 状态和日志追溯字段不足。

## 修订与复核

- `WidgetWork.kt` 现在用完整 `presentationOccurrences + recordedEvents` 派生一次，再按
  occurrence ID 选择目标项，未改变领域匹配优先级、时间窗口或 null-slot fallback。
- `WidgetWorkTest` 新增同一计划 O1 legacy/null-slot 已记录、O2 当前 `DUE`、反向事件/计划
  输入顺序的回归；O2 成功写入且不重复。
- Android 回归新增 OPEN_APP fill-in 只读 identity、`MainActivity` 路由和 quick-action 工厂
  零调用断言；`TODO.MD`、`V16_B_EVIDENCE.md`、`V16_ACCEPTANCE.md` 已同步当前阶段事实。
- JVM 31 项、Android 19 项均以 exit code 0 通过；可追溯日志为
  [B-03-gradle-2026-09-06.log](B-03-gradle-2026-09-06.log) 和
  [B-04-android-2026-09-06.log](B-04-android-2026-09-06.log)。

## 独立复审结论

ChatGPT 于同一连接入口完成第二次只读复核，结论为：

**`APPROVE V1.6-B FIRST SLICE`**

| 级别 | 结果 |
|---|---|
| P0 | 无 |
| P1 | 无；完整 occurrence 上下文与 legacy/null-slot sibling 问题已关闭 |
| P2 | 无；OPEN_APP 只读路由、TODO 状态和日志证据已关闭 |
| P3 | 初始日志的 `Result` 行缺少空行，已修复；不影响测试结果 |

该批准只覆盖 B 阶段首批 action/configuration slice。A、B 仍保持 `IN_PROGRESS`；Wear 字段
与版本兼容矩阵、C renderer、真实 launcher/宿主和阶段关闭审阅仍未完成。

## B-06 Wear 契约矩阵首轮复审

独立复审结论为 `REQUEST_CHANGES`，无 P0。生产协议和同步代码未发现需要立即修改的问题，
但首轮指出以下契约/证据缺口：

- 矩阵把已有 v1 snapshot 的 v1.5 Phone 误写成可能没有 snapshot，且缺少 v1.6 Phone →
  v1.5 Wear App 行；
- `todaySummary` 的本地日期、计算时间、完成/总数、无计划与无 occurrence 状态以及截断标识
  尚未定义到可执行程度；
- `WearAppCompatibilityMatrixTest` 有恒真路径断言和过度具体的版本配对测试名称，不能把
  codec/presentation fixture 当成实体 Data Layer 配对；
- B-05 日志中的核心任务为 `UP-TO-DATE`，不能证明本轮全部测试 fresh 执行；验收表 B-04
  还有“独立复审待执行”的陈旧措辞。

## B-06 修订与复审请求

已完成修订：

- [V16_B_WEAR_MATRIX.md](V16_B_WEAR_MATRIX.md) 现在区分 v1.5 baseline、summary 扩展前后
  的 v1.6 Phone、v1.5/v1.6 Wear、pre-v1 无 snapshot，并明确矩阵只是契约预期；
- 冻结 `todaySummaryState`、`todayLocalDate`、`computedAt`/`generatedAt`、
  `completedCount`/`totalCount` 和首个扩展不发布 `truncated` 的语义；
- 删除恒真断言，加入“已知 v1 snapshot 在缺少 summary 时仍可展示”和未来 tag 11 被旧 v1
  decoder 跳过的测试；
- 使用 `--rerun-tasks` fresh 执行 45 项，0 失败，并将 HEAD、工作树、完整命令、测试数和
  BUILD SUCCESSFUL 写入 [B-05-wear-contract-2026-09-06.log](B-05-wear-contract-2026-09-06.log)。

## B-06 修订后独立复审结论

ChatGPT 于同一 custom-domain connector 完成修订后的只读闭环复核，结论为：

**`APPROVE B-06 TECHNICAL CONTRACT CHECKPOINT`**

| 级别 | 结果 |
|---|---|
| P0 | 无 |
| P1 | 无；v1.5 baseline、v1.6 Phone/Wear、legacy Tile 和 pre-v1 配对语义已关闭 |
| P2 | 无；todaySummary 语义、测试边界和 45 项 fresh 证据已关闭 |
| P3 | 无；B-04 状态和 codec 测试名已同步 |

复审确认：v1.5 已具备 v1 snapshot/request/store/decoder；缺少 `todaySummary` 只表示汇总不可用；
JVM fixture 不冒充实体 Data Layer 配对；tag 11 可被旧 v1 decoder 跳过；最新日志使用
`--rerun-tasks`，45 项通过，54 个任务执行，exit code 0，`BUILD SUCCESSFUL`。

该批准只覆盖 Wear 字段设计、兼容矩阵和测试证据。随后开始的 B-08 `todaySummary` 可选
tag 11 实现不在本次批准范围内；其编码/解码、Phone 权威汇总生成、真实旧/新 APK 与 Data Layer
配对、confirmation/undo 回执兼容和 B 阶段最终关闭仍需独立证据。A、B 继续保持 `IN_PROGRESS`。

## B-08 `todaySummary` 候选实现状态

当前工作树已加入可选 tag 11 的 model、codec 和 Phone builder 派生：

- `WearAppTodaySummary` 固化 `todayLocalDate`、`computedAt`、`completedCount`、`totalCount`
  和 `NO_ENABLED_PLANS`/`NO_OCCURRENCES`/`HAS_OCCURRENCES` 三态；首个扩展不发布 `truncated`。
- Phone 从完整当日 occurrence 集合计算 summary，已有 v1 字段、legacy path、确认/撤销和协议版本
  均未改变。
- 首轮复审指出的跨午夜匹配上下文、超过 5 条 upcoming 截断边界和 tag 11 约束测试缺口已修订：
  builder 现在按 `matchBefore + matchAfter` 扩展相邻日期匹配上下文后再按本地日期计数，summary 总数不受 upcoming 上限影响，
  codec 增加日期/计数/状态及 duplicate/malformed tag 11 拒绝回归。
- 新增确定性回归覆盖前一天 22:00、今日 00:00 与 23:00 legacy null-slot event 的歧义，确保不误计今日完成。
- 修订后全量 Experience Core、Phone、Wear JVM fresh rerun 为 92 suites / 815 tests，0 failures、
  0 errors；日志见 [B-08-wear-summary-2026-09-06.log](B-08-wear-summary-2026-09-06.log)。

B-08 当时仍待下一轮独立只读复审；后续第三次复审结论见本文末尾，真实旧/新 APK/Data Layer
组合和发布候选验证仍属于后续门禁。

## B-08 第二轮复审发现与再修订

第二轮只读复审仍未批准 B-08，结论为 `REQUEST_CHANGES`，无 P0。复审指出首次修订只按单侧
`matchBefore`/`matchAfter` 扩展上下文，仍可能排除位于两个窗口之和之外的歧义 occurrence；同时
此前文档声称有跨午夜回归，但测试尚未覆盖该反例。

已完成再修订：

- `WearAppSnapshotBuilder` 现在以 `matchBefore + matchAfter` 向当天起止两侧扩展匹配上下文；
- `WearAppSnapshotBuilderTest` 新增前一天 22:00、今日 00:00 和 23:00 legacy null-slot event 的
  确定性歧义测试，完成数保持为 0；
- fresh JVM 回归为 92 suites / 815 tests，0 failures、0 errors，日志已刷新。

该再修订随后进入第三次范围收窄的只读复审；在复审完成前，B-08、B 阶段和发布资格继续保持未关闭。

## B-08 第三次独立只读复审结论

2026-09-06，ChatGPT 通过固定域名连接器
`Codex with ChatGPT · Evolune-v1.2 · yingqiu.me` 完成只读复审。复审先确认工作区为
`Evolune-v1.2`、分支 `main`、HEAD `df12329278eafa488713edf202554cdbd523b8d0`，工作树
dirty 且无冲突；随后沿当前调用链核对实现、测试、协议边界和五份 B-08 证据文件，未修改文件、
未提交、未执行写操作。

| 级别 | 结果 |
|---|---|
| P0 | 无 |
| P1 | 无；`matchBefore.plus(matchAfter)` 上下文与跨午夜歧义回归成立 |
| P2 | 无；六条 occurrence 的 `upcoming=5` / `total=6` 边界、tag 11 约束和证据一致性成立 |
| P3 | 无 |

复审确认 legacy `/hrt/*`、confirmation/undo、producer/revision、Data Layer handler 和协议版本
边界未被本批修改；日志中的 `--rerun-tasks`、92 suites / 815 tests、0 failures / 0 errors、
54 tasks executed 和 `BUILD SUCCESSFUL` 与工作树一致。独立复审结论为：

**`APPROVE B-08 CANDIDATE IMPLEMENTATION`**
**`C2C DONE`**

该批准只覆盖 B-08 候选实现、其 JVM 证据和协议边界。B 阶段仍为 `IN_PROGRESS`；真实旧/新 APK 与
Data Layer 配对、Wear UI 展示、确认/回执实机验证、候选提交和发布批准仍是后续门禁。
