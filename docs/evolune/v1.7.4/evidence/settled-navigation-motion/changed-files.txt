v1.7.4 settled navigation motion — corrected changed files
==========================================================

Correction delta from c8f5edff:
  M navigation/AppNavigation.kt
      remember(currentRoute) reads preview; SideEffect calls commit.
  M ui/motion/SettledGeometryPageMotion.kt
      RouteEdgeTracker is now a pure preview / idempotent commit model.

Tests:
  M ui/motion/SettledGeometryPageMotionTest.kt (8 tests; +3 lifecycle proofs)
  M ui/screens/MotionListUxBoundaryTest.kt (safe preview/SideEffect guard)

Correction-zero files: EvolunePageMotion.kt, EditorTransitionHost.kt,
PrimaryNavigationChrome.kt, all product features, backup/theme/Settings/Widget/
Wear/HC/Drive/Room, version.

The authoritative implementation.diff is against bcb12d4 and excludes docs.
