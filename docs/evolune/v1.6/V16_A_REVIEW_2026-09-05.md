# v1.6-A 独立只读审阅记录

日期：2026-09-05
审阅者：已连接工作区的 ChatGPT 独立只读会话
范围：`V16_SPEC.md`、`V16_ACCEPTANCE.md`、`V16_PLAN.md`、`V16_REVIEW.md`、
`CURRENT_STATUS.md`、现有 Phone Widget/Wear 实现
方式：独立读取文档、沿 selector/UI/action/协议调用链核对；未修改工作区文件

## 结论

`REQUEST_CHANGES`。没有 P0；A 阶段继续保持 `IN_PROGRESS`。

## 发现的问题

### P1

- `next_dose` 的显示对象和可确认对象没有冻结。未来 `UPCOMING` occurrence 可以被 selector
  选出，现有 `WidgetUiMapper` 又把它映射为 `RECORD`，但
  `ContractWidgetQuickActionWork` 会拒绝 `scheduledLocalDate` 不是当前本地日期的命令。
  因此“明日下一次服药”可能出现按钮存在但确认必失败的状态。
- 修订动作：规格现在要求只对 `MedicationActionAvailability.AVAILABLE` 且日期检查通过的 occurrence
  显示确认；未来/跨日/未到 due 只读并进入 App。B/C 必须修正 `UPCOMING → RECORD` 映射并加入测试。

### P2

1. 原 `V16_ACCEPTANCE.md` 表缺少起点/候选提交、设备与数据集、证据路径、审阅者/日期、问题与复核字段。
   现已按 `V16_REVIEW.md` 的最小结构重写。
2. A-02/A-03 只有摘要，没有可定位日志。现已保存 [A-03-gradle-2026-09-05.log](A-03-gradle-2026-09-05.log)。
3. v1.5 `Energy/background` 是 `SKIPPED_BY_OWNER`，真实 battery/Wear active-background 仍未验证；
   现已写入 [V16_SPEC.md](V16_SPEC.md) 和 [V16_A_EVIDENCE.md](V16_A_EVIDENCE.md)，明确不继承为 v1.6 PASS。
4. 样式选择入口和旧实例缺失 `styleId` 的回退未冻结。现已冻结为继续使用现有配置页，
   缺失/未知值映射到 `legacy_default`，B 阶段用迁移测试锁定。

### P3

- `V16_REVIEW.md` 顶部状态与 A 已开始的事实不一致，已改为 A `IN_PROGRESS`、B–G 未开始验收。
- v1.5 性能数字应描述为同一 emulator 会话的重复采样基线（2 次 warm-up、7 次 measured passes），
  已在规格和证据中修正措辞。
- [CURRENT_STATUS.md](../CURRENT_STATUS.md)、[ROADMAP.md](../ROADMAP.md) 和 [TODO.MD](../../../TODO.MD)
  的 v1.5/v1.6 里程碑状态已同步，避免把 v1.5 误读为尚未开始。

## 可批准部分

- `v1.5.0` tag、HEAD、Phone/Wear versionCode、SDK 和包身份一致。
- Phone Room/domain/repository 作为唯一事实来源的边界正确；没有引入第二个 Room、第二套 PK 或 Widget/Wear 事实源。
- occurrence identity、Widget 批量共享快照、按 `appWidgetId` 的外观配置以及旧 Wear `/hrt/*` 路径盘点方向正确。
- Phone 五类展示、Wear 三 Tile、三 Complication、设计尺寸目标和 v1.5 基线数值与计划及现有实现一致。
- 当前没有提前宣称 A `DONE`；缺少的真实宿主、性能和产品证据仍被标记为未完成。

## A 阶段缺失证据

- due/upcoming/跨日/同一时刻多 occurrence 的 `next_dose` 语义和动作测试。
- E2 stale/trend、PK 图表窗口/采样上限、宿主展示延迟上限。
- 全部状态稿、真实 min-dp/resize、两个 Launcher、API 31/36 门槛、真实圆形 Wear 和 Complication 表盘。
- `EMPTY/STEADY/DENSE × 1/3/5 Widget`、多 Wear surface 以及 PK/Data Layer/wakeup/真实刷新延迟的重复采样。
- 修订后的独立复审结论和必要的产品/视觉批准。

## 复审要求

修订后需重新读取规格、验收表和受影响的 Widget action 链，独立重跑关键 JVM 测试；在 P1 清零、
P2 必交付证据补齐且产品/视觉证据形成前，不得把 A 标为 `DONE`，也不得进入 B 以外的 UI 接入。

## 修订后独立复审（2026-09-05）

本节记录第二次只读会话对修订后文档的检查。该会话读取了 `V16_SPEC.md`、`V16_ACCEPTANCE.md`、
`V16_A_EVIDENCE.md`、本文件、`V16_PLAN.md`、`V16_REVIEW.md`、`CURRENT_STATUS.md`、
`ROADMAP.md` 和 `TODO.MD`，并再次沿 Phone/Wear 动作调用链核对；未修改工作区文件。

### 结论

`REQUEST_CHANGES`。没有 P0；Phone 侧上一轮 P1 已修正，但 A 阶段仍不能关闭。

### 剩余问题

- **P1：Phone/Wear 动作语义尚未形成同一张兼容矩阵。** Phone `next_dose` 已明确只对
  `MedicationActionAvailability.AVAILABLE` 且日期检查通过的 occurrence 提供确认；现有 Wear
  `WearAppStore.canConfirm()` 仍允许 `UPCOMING` 或 `DUE`。复审要求 A 阶段明确选择并记录这个
  跨端差异，以及旧 Phone/新 Wear、新 Phone/旧 Wear、两端新版的版本协商和回执测试。
- **P2：状态文档顶部仍有公开版本漂移。** `CURRENT_STATUS.md` 顶部仍写 v1.4.0，
  `ROADMAP.md` 仍从 v1.4.0 开始叙述，后文却已经把 v1.5.0 作为 sealed baseline、v1.6-A
  作为 active；需要把当前公开版本和历史里程碑层级统一。
- **P2：缓存边界用词过宽。** 原规格写成“不得建立缓存”，但现有 Wear 使用可重建的
  `WearAppStore`/`WearPlanStore` 派生 snapshot/cache。应改为禁止新增独立权威事实来源，允许
  复用带 freshness/producer identity 的派生缓存。
- **P3：关键 JVM 日志主要是 `UP-TO-DATE`，还不能替代独立 fresh rerun；TODO 中 v1.3
  的未完成标记也应与当前已有 Wear App 实现对齐。**

### 可以批准的部分

上一轮的 Phone `next_dose` 修订、验收表字段、A-02/A-03 日志路径、`SKIPPED_BY_OWNER` 继承、
`legacy_default` 回退、系统 picker 决策、`V16_REVIEW.md` 状态头和重复采样措辞均已确认落地。
基线身份、单一事实来源、Widget/Wear inventory 方向、范围和性能数字仍与实现及 v1.6 计划一致。

### A 阶段仍缺的证据

跨 Phone/Wear 动作契约和旧/新组合测试、due/upcoming/跨日/DST/timezone 测试、四个产品数值阈值、
完整状态稿和真实尺寸、API 31/36 与真实宿主矩阵、Wear 真表/Complication 表盘、1/3/5 Widget
及多 Wear surface 性能采样、PK/Data Layer/wakeup/真实刷新延迟、独立 fresh 测试复跑、A 候选提交
及 exact diff、A-05/A-06/A-07 关闭记录和最终技术/产品审阅仍未完成。

### 复审后处理

本轮复审意见已写回 `V16_SPEC.md`、`V16_PLAN.md`、`V16_ACCEPTANCE.md`、`V16_A_EVIDENCE.md`、
`CURRENT_STATUS.md`、`ROADMAP.md` 和 `TODO.MD`。在再次独立复审确认文档一致性后，A 仍需补齐
上述运行和宿主证据；本文件历史结论不把 A 标为 `DONE`。

## 第三次独立只读复审（2026-09-05）

### 结论

`REQUEST_CHANGES`。没有 P0；A 阶段继续保持 `IN_PROGRESS`。

### 已确认通过

- Phone Gallery 采用 `AVAILABLE`、Wear App/新 Wear Tile 保留 v1.3 `UPCOMING/DUE` 的有意跨端版本化动作边界已经在 `V16_SPEC.md` 的 authoritative action matrix、`V16_PLAN.md` 的 A/E 规则中对齐。
- `CURRENT_STATUS.md` 与 `ROADMAP.md` 顶部已统一为 v1.5.0 sealed stable baseline、v1.6-A active；`TODO.MD` 的 v1.3 历史状态也与已有 Wear 实现对齐。
- 规格已明确“不新增独立权威事实来源”，同时允许复用带 freshness/producer identity 的 `WearAppStore`/`WearPlanStore` 可重建派生 snapshot/cache；Phone 仍是唯一事实来源。
- [A-03-gradle-2026-09-05-rerun.log](A-03-gradle-2026-09-05-rerun.log) 可定位且为 fresh rerun：`BUILD SUCCESSFUL`、`55 actionable tasks: 55 executed`；日志不能单独证明执行者独立性，故记录为 fresh 而非 independent execution。

### 剩余问题

- **P1：Phone `AVAILABLE` 还必须是 action-time 双重门槛。** 目前规格已经冻结“渲染时只显示
  `AVAILABLE`”，但现有 `ContractWidgetQuickActionWork`/`LocalActionRecorder.recordWidget()` 尚未在点击时完整重检
  availability/due-window。B/C 必须补上旧/stale PendingIntent、渲染后变为 `WINDOW_EXPIRED`、同日
  `UPCOMING`、已删/已改方案的拒绝路径；失败不得落库或先显示成功。
- **P2：** `CURRENT_STATUS.md` 原有的 Wear limitation 已修正为当前已有的 versioned snapshot、receipt/undo、producer identity 和可重建 cache；该描述需由后续候选提交复核，不得再回退为“没有完整 Wear application experience”。
- **P3：** `ROADMAP.md` 的历史条目已改为“该版本已公开发布并封存”；`V16_A_EVIDENCE.md` 已去掉无法由日志证明的“独立 fresh rerun”表述。

### A 阶段仍缺的证据

除上轮列表外，仍需提供 Phone action-time gate 的确定性测试与调用链证据。其余未完成项包括：due/upcoming/跨日/DST/timezone、旧/新 Phone/Wear 组合与回执、四个产品数值阈值、完整状态稿和真实尺寸、API 31/36、真实 Launcher/圆形 Wear/Complication 表盘、1/3/5 Widget 与多 Wear surface 性能、PK/Data Layer/wakeup/真实刷新延迟、A-05/A-06/A-07 关闭记录、候选提交 exact diff，以及最终技术/产品独立批准。

### 复审后处理

本次意见已写回 `V16_SPEC.md`、`V16_PLAN.md`、`V16_ACCEPTANCE.md`、`V16_A_EVIDENCE.md`、
`CURRENT_STATUS.md` 和 `ROADMAP.md`。本次只完成规格和证据边界修订，没有提前实现 Phone action-time gate，
也没有把 A 标为 `DONE`。
