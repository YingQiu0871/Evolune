v1.7.4 settled navigation motion — pre-correction frame evidence
================================================================

This is candidate c8f5edff evidence. It is retained for history and is not a
post-correction visual claim.

Captured on the physical Pixel 11 Pro with a slowed-animation attempt
(animator_duration_scale=10) and a burst of device screenshots
(screenshots/frame-forward-*.png, screenshots/frame-back-*.png).

Forward: History -> Insights
  frame-forward-1 (initial): chrome already at final geometry; content at low
  alpha and slightly inset. Frames 2..3 progressed; frames 4..8 settled.
Back: Insights -> History
  frame-back-1 (initial): chrome restored at final geometry; content at low alpha
  and slightly inset. Frames 2..3 progressed; frames 4..8 settled.

No historical frame showed container resizing, a full-size flash followed by a
snap to 0.98, a scale-out tail, or chrome scaling. These observations belong to
the pre-correction candidate only.
