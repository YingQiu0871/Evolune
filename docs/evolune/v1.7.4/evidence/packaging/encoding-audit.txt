v1.7.4 packaging — encoding audit
=================================

Scope: every file in this evidence directory (recursive), MANIFEST.sha256
generated after this audit.

Correction note: the original packaging commit (8e28fce) captured the two raw
Gradle logs with Windows CRLF in the worktree while their committed Git blobs
were LF-normalised by core.autocrlf. That disk != blob condition is repaired by
the evidence-only follow-up commit "docs(v1.7.4): normalize packaging evidence
bytes": validation-task.log and release-build.log were normalised to LF-only
with all textual content preserved exactly (verified line-by-line; no
timestamp, result, or semantic change).

All text evidence now: valid UTF-8, no UTF-8 BOM, no UTF-16 BOM, no NUL, CR=0
(LF only), disk bytes == HEAD blob bytes.

Files (text):
  identity.txt, baseline.txt, approved-implementation.txt, version-bump.txt,
  changed-files.txt, frozen-source-audit.txt, phone-apk-metadata.txt,
  wear-apk-metadata.txt, certificate-continuity.txt, artifact-hashes.txt,
  release-smoke-phone.txt, release-smoke-wear.txt, packaging-diff.txt,
  encoding-audit.txt, implementation.diff, validation-task.log,
  release-build.log, logs/physical-walkthrough.log, MANIFEST.sha256

Binary: screenshots/*.png, release-wear-launch.png — PNG payloads, exempt from
text encoding rules, byte-identical to the original packaging commit; hashed in
MANIFEST.sha256.

Result: PASS.
