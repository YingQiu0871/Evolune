EVOLUNE v1.8.0 B-03 — LOGS: TRACKED-LF PROCEDURE
================================================

Fresh worktree checkout uses core.autocrlf=true (repo-local git config), so
tracked text files are materialized with CRLF while the tracked blobs are LF.

Byte-sensitive files for this slice:
- app/src/test/resources/phase-e/golden-canonical.json  (blob 3531 B LF)
- app/src/test/resources/phase-e/golden-canonical.csv   (blob 1763 B LF)
- app/src/main/java/.../navigation/AppNavigation.kt     (blob 69129 B LF;
  raw checkout 70686 B CRLF)

Procedure applied for the canonical run:
1. save raw checkout bytes (JSON/CSV were already LF; AppNavigation.kt 70686 B CRLF)
2. write tracked LF blob bytes via: git cat-file blob HEAD:<path> > <path>
3. run the full JVM regression (counts recorded in full-jvm-summary.txt)
4. restore the saved raw checkout bytes
5. git status remains clean (autocrlf normalizes on comparison)

Observed pre-existing finding:
- pristine checkout: HistoryLazyEntryArchitectureTest fails
  (AppNavigation.kt raw = HistoryScreen(<CR><LF>; test needle embeds "\n")
- canonical tracked-LF: test passes
- B-02 worktree: test passed because its edited region carried LF-only endings
  (13 LF-only lines). Not caused by B-03; no B-02 file was modified.
