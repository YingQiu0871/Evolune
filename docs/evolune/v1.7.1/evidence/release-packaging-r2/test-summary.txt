V1.7.1 release packaging R2 — fresh JVM regression and follow-up verification
=============================================================================

All runs used --rerun-tasks from source HEAD 06eb217; raw logs show actual execution (no
UP-TO-DATE or FROM-CACHE test task presented as fresh). Counts parsed from the final JUnit XML
of each module.

App JVM
  command: .\gradlew.bat :app:testDebugUnitTest --rerun-tasks
  log    : logs/app-jvm.log (28 actionable tasks: 28 executed)
  result : suites=137  tests=1343  failures=0  errors=0  skipped=0

experience-core JVM
  command: .\gradlew.bat :experience-core:test --rerun-tasks
  log    : logs/experience-core-jvm.log (4 actionable tasks: 4 executed)
  result : suites=20  tests=171  failures=0  errors=0  skipped=0

Wear JVM
  command: .\gradlew.bat :wear:testDebugUnitTest --rerun-tasks
  log    : logs/wear-jvm.log (26 actionable tasks: 26 executed)
  result : suites=11  tests=90  failures=0  errors=0  skipped=0

Version guard: .\gradlew.bat validateEvoluneIdentityAndVersioning — BUILD SUCCESSFUL
(logs/version-guard.log).

History follow-up verification (packaging provenance, not a new implementation review)
  Approved calendar-fix HTML marker "history-day-number-" is present in the NEW signed Phone
  release APK dex (verified by scanning the APK classes*.dex payloads).
  Source provenance is 06eb217e65612e3ceb0f116ed0265fa1a8bbfcd9 (parent chain includes the
  approved fix). No superseded APK was used.

R2 packaging instrumentation reference (executed at 06eb217 on emulator-5554; per-class
counts parsed from the executed JUnit XML — the correct breakdown):
  HistoryCalendarGeometryTest    :  4
  HistoryScreenTest              : 21
  HistoryNavigationTest          :  4
  HistoryVisualEvidenceTest      :  1
  TOTAL                          : 30  (0 failures, 0 errors)
  Note: the historical ui-hotfix-followup evidence prose listed 4/13/6/7; that by-class prose
  was a mislabel. The bundle remains untouched as historical evidence; R2 evidence uses the
  independently verified correct breakdown above.

Known P3 items carried (non-blocking, none caused by this packaging pass):
  - historical follow-up evidence per-class prose mislabel (corrected here for current use)
  - History calendar effective plain-clickable height is approximately 36.19dp (width ~52.6dp);
    no automatic 48dp expansion is established — future accessibility polish
  - inherited PK tooltip i18n polish
  - JUnit evidence self-containment note where applicable
