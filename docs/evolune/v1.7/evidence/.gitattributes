# Evidence bytes must be stored verbatim: SHA-256 manifests in this directory
# are only reproducible if Git performs no EOL conversion on these files.
# Raw logs/XML are captured artifacts: their trailing whitespace is part of the
# recorded bytes, so whitespace linting must not flag them in `git diff --check`.
* -text -whitespace
